# Resource archive moved

The ZIP was a duplicate archive of `PRI-General_available_resources/`. Its useful raw resources are now under `data/cache/sources/`, and the duplicate archive was removed. Files larger than 1 GB remain local and are excluded from Git by the v2 ignore rules.
