# v2 data dictionary

The canonical fields are defined in `src/pri_general/constants.py`. The release tables will use one row per mother sample and include source/evidence, construct, structure/interface QC, family assignments, and split fields.

This file is a release placeholder until a strict `READY` manifest is produced.
