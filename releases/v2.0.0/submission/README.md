# Prediction submission forms

The canonical prediction submission template is `prediction_submission_template.csv` in this directory. The completed file, when returned from the external Boltz-2 run, is `prediction_submission.csv`.

Required identity fields are `sample_id`, `protein_id`, `rna_id`, `structure_path`, and `confidence`. Boltz-2 and MSA execution are outside this local rebuild.
