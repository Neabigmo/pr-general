# Releases

Each release directory contains metadata and, after strict validation, the final tables and submission files. Raw data and generated intermediates remain in the local cache.

`v2.0.0` is currently a draft until family assignments, explicit interface QC, and the externally produced Boltz results are available.
